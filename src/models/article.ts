export interface Article {
    id: string
    title: string
    summary: string
    content?: string
    image?: string
    pubDate: string
    link: string
    source: string
} 

export interface Summary {
    id: string;
    summary: string;
    image: string;
  }